{
  "name": "the-scrum-book",
  "private": true,
  "version": "0.0.0",
  "scripts": {
   "scripts": {
  "dev": "npm run clean && next dev",
  "build": "next build",
  "start": "next start",
  "lint": "next lint",
  "test": "jest",
  "clean": "rm -rf .next"
},
  }
}
