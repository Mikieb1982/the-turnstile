// postcss.config.js
// Tailwind CSS is no longer required for the Genkit-ready shell, so we keep the
// PostCSS pipeline minimal to avoid depending on packages that aren't available
// in this environment.
export default {
  plugins: [],
}
